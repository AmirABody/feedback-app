import { FcAbout } from "react-icons/fc";

const AboutPageButton = () => {
  return (
    <div className="flex items-center justify-center w-10 aspect-square bg-white border-gray-400 border-2 rounded-full">
      <FcAbout size={18} />
    </div>
  );
};

export default AboutPageButton;
